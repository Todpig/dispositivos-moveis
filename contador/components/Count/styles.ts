import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  Container: {
    borderRadius: 100,
    backgroundColor: "#fff",
    fontSize: 32,
    width: 128,
    height: 128,
    alignItems: "center",
    justifyContent: "center",
    padding: 32,
  },
  Text: {
    fontSize: 32,
    fontWeight: "bold",
    color: "#000",
  },
});
